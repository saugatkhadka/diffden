{
  "name": "diffden",
  "version": "0.1.0",
  "description": "DiffDen — TUI file version tracker that snapshots watched files into an internal git repo",
  "module": "src/index.ts",
  "type": "module",
  "bin": {
    "diffden": "./bin/cli.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun run --watch src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@opentui/core": "^0.1.77",
    "chokidar": "^5.0.0",
    "simple-git": "^3.30.0"
  }
}
